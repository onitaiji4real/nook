# Docker

Phase 1 uses the root `docker-compose.yml` for local PostgreSQL 16 + PostGIS 3.5.

Application production Dockerfiles are delivered by P1-005 together with image scanning and Cloud Run deployment.
